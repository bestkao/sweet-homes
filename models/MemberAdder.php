<?php

/**
 * This is a class MemberAdder that extends the class UserAdder.
 * This class handles the responsibility of adding data to the 
 * Database of a MemberModel.
 *  
 *  Example:-
 *           ...
 *           public function addMember($data)
 *           {
 *              ...
 *              $this->memberAdder->add($data, $database); 
 *           }
 * @author justina
 */
final class MemberAdder extends UserAdder
{
    
}
